# Node.js
node_modules/

# Env / secrets
.env
.env.local
.env.*.local

# Vercel
.vercel

# Local SQLite (legacy; app uses Neon Postgres)
data/*.sqlite
data/*.sqlite-journal

# Local agent / product docs (keep on disk; do not commit)
AGENTS.md
doc/

